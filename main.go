package main

import (
	"fmt"
)

func main() {
	fmt.Println("test...")
}

func Qs(arr []int, begin, end int) {
	if begin < end {
		left := begin
		right := end
		tmp := 0
		for left != right {
			//1.取第一个元素为基准值
			tmp = arr[left]

		}

	}
}
